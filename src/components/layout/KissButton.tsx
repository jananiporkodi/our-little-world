// Replaced by the floating ReactionDock (kiss/hug/miss-you/high-five with sent counters).
// Kept as an empty module rather than deleted - the sandbox can't remove files from the
// repo directly. Safe to `git rm src/components/layout/KissButton.tsx` in your next commit.
export {};
